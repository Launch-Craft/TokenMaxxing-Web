# TokenMaxxing landing page — environment

# Base URL of the dedicated backend (../backend)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787

# Fallback download link if the backend is unreachable
NEXT_PUBLIC_DOWNLOAD_FALLBACK=https://github.com/acmediamarketing/tokenmaxxing/releases/latest
